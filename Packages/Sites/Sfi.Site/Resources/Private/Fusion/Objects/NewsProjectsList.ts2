prototype(Sfi.Site:NewsProjectsList) < prototype(Flowpack.Listable:List) {
	@context.listTitleClass = 'H1'
	@context.listTitle = Neos.Fusion:Tag {
		tagName = 'a'
		content = 'Проекты'
		attributes.href = NodeUri {
			node = ${q(site).children('[uriPathSegment = projects]').get(0)}
		}
	}
	@context.archiveLink = ${q(site).children('[uriPathSegment = projects]').get(0)}
	@context.archiveLinkTitle = 'Все проекты'
	@context.archiveLinkAdditionalParams = ${{'--listable-paginate': {'@action': 'index'}}}
	@context.archiveLinkClass = 'ArchiveLink--main'
	@context.wrapClass = 'position-relative'

	prototype(Flowpack.Listable:Listable) {
		@context.listClass = 'row small-up-1 medium-up-1 large-up-2'
		@context.itemClass = 'column'
		@context.limit = 2
		@context.paginationEnabled = ${false}
		@context.itemRenderer = 'Sfi.Site:NewsStandardShort'
		collection = ${q(site).children('[uriPathSegment = projects]').find('[instanceof Sfi.Site:News]').get()}
	}
	@cache {
		mode = 'cached'
		entryTags {
			1 = ${'NodeType_Sfi.Site:News'}
		}
	}
}
